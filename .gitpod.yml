# Gitpod 配置文件 - AI 优化版
# 访问方式：在 GitHub 仓库 URL 前添加 gitpod.io/#

image: gitpod/workspace-python-3.11

# 任务配置
tasks:
  - name: API Server
    init: |
      echo "🚀 初始化 AI Cloud IDE..."
      pip install --upgrade pip requests
    command: |
      echo "🌐 启动 API 服务..."
      cd /workspace/ai-cloud-ide
      python api_server.py

  - name: Terminal
    command: |
      echo "✅ AI Cloud IDE 已就绪!"
      echo ""
      echo "📌 快速开始:"
      echo "   python main.py      - 运行示例程序"
      echo "   python api_server.py - 启动 API 服务"
      echo ""
      echo "🌐 API 端口会自动转发，查看 'Ports' 标签获取 URL"

# 端口配置 - 自动转发
ports:
  - port: 8080
    visibility: public
    name: API Server
    description: AI 访问 API
  - port: 3000
    visibility: public
    name: App Server
  - port: 8888
    visibility: public
    name: Jupyter

# VS Code 扩展
vscode:
  extensions:
    - ms-python.python
    - ms-toolsai.jupyter
    - dbaeumer.vscode-eslint
    - esbenp.prettier-vscode
    - eamodio.gitlens
    - github.copilot

# 工作区配置
workspaceLocation: "/workspace/ai-cloud-ide"

# 启动配置
checkoutLocation: "/workspace/ai-cloud-ide"
